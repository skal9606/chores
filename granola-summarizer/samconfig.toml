# SAM CLI deployment configuration
# Run: sam build && sam deploy

version = 0.1

[default.deploy.parameters]
stack_name = "granola-summarizer"
resolve_s3 = true
s3_prefix = "granola-summarizer"
region = "us-west-2"
confirm_changeset = true
capabilities = "CAPABILITY_IAM"
